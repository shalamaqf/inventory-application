<nav>
    <ul>
        <% if (genreList) { %>
            <% genreList.forEach(genre => { %>
                <li><%= genre.name %></li>
                <%- include("./updateAndDeleteGenre", { genre }) %>
            <% }) %>
        <% } %>
    </ul>
</nav>